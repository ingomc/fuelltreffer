# Dokploy specific configuration
# This file contains the environment variables needed for Dokploy deployment

HOST=0.0.0.0
PORT=4000
NODE_ENV=production

# Dokploy Setup Checklist:
# 1. In Dokploy Application Settings:
#    - Port: 4000
#    - Protocol: HTTP (Dokploy handles HTTPS termination)
#    - NO Health Check needed! 
#
# 2. In Dokploy Domain Settings:
#    - Domain: fuelltreffer.andre-bellmann.de
#    - Target Port: 4000
#    - SSL: Let's Encrypt enabled
#    - Redirect HTTP to HTTPS: enabled
#
# 3. Environment Variables in Dokploy:
#    HOST=0.0.0.0
#    PORT=4000
#    NODE_ENV=production
#
# 4. Make sure the container exposes port 4000 and binds to 0.0.0.0

# Troubleshooting:
# - If domain doesn't work but IP:4000 does, check proxy settings
# - Ensure Dokploy proxy is pointing to the correct container port
# - Check if firewall allows traffic on port 4000
